<%= form_with model: @todo, local: true do |f| %>

    <%= f.label :title, "Título del todo" %>
    <%= f.text_field :description %>

    <%= f.label :content, "Content" %>
    <%= f.text_area :completed %>

    <%= f.submit "Enviar" %>
    <% end %>